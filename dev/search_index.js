var documenterSearchIndex = {"docs":
[{"location":"#CropPhenoFromDrones","page":"Home","title":"CropPhenoFromDrones","text":"Documentation for CropPhenoFromDrones.\n\n","category":"section"},{"location":"#CropPhenoFromDrones.load_raster-Tuple{String}","page":"Home","title":"CropPhenoFromDrones.load_raster","text":"load_raster(path::String)::Raster\n\nLoad a raster from the file at the specified path and return a Raster object.\n\nArgument\n\npath::String: Path to the raster file to be loaded.\n\nReturns\n\nRaster: A Raster instance constructed from the file at path.\n\nThrows\n\nan error if the file does not exist or cannot be opened.\n\nNotes\n\nThis function is a thin wrapper around the Raster constructor and performs no additional validation or processing.\n\nExample\n\njulia> raster = simulate_raster();\n\njulia> fname_raster = write_raster(raster, path=\"simulated.tiff\", overwrite=true);\n\njulia> raster_reloaded = load_raster(fname_raster);\n\njulia> raster_reloaded == raster\ntrue\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.load_shapes_merge_layout-Tuple{String}","page":"Home","title":"CropPhenoFromDrones.load_shapes_merge_layout","text":"load_shapes_merge_layout(\n    fname_shapes::String;\n    fname_layout::Union{Nothing,String}=nothing,\n    id::String=\"id\",\n    name::String=\"name\",\n    remove_rows_with_missing::Bool=true,\n)::DataFrame\n\nLoad shapes from a geospatial file and optionally merge them with a layout table by id.\n\nArguments\n\nfname_shapes::String: path to the shapes file that GeoDataFrames.read can consume, typically a Shapefile.\nfname_layout::Union{Nothing,String}=nothing: optional path to a CSV/TSV layout table to merge with the shapes; pass nothing to skip merging.\nid::String=\"id\": column name used as the join key in both the shapes and layout tables.\nname::String=\"name\": column name in the layout containing entry or genotype names that will be renamed to \"name\" to satisfy Shapefile field-length limits.\nremoverowswith_missing::Bool=true: if true, disallowmissing! is applied to the merged DataFrame to remove or convert missing values following the join.\n\nReturns\n\nDataFrame containing the shapes read from fnameshapes if fnamelayout is nothing, otherwise the left-joined DataFrame of shapes and layout on the id column with layout fields appended.\n\nErrors\n\nThrows an ErrorException if the id column is absent in the shapes file.\nThrows an ErrorException if either the id or the specified name column is absent in the layout file when a layout file is provided.\n\nBehaviour and notes\n\nThe function uses GeoDataFrames.read to read geospatial shapes and CSV.read to read the layout table.\nWhen a layout is provided the specified name column is renamed to \"name\" because Shapefile field names are limited in length.\nAll columns in the layout that contain AbstractString values are converted to plain String to improve Shapefile compatibility.\nThe merge is a left join on the id column and makeunique=true is used to avoid duplicate column names.\nIf removerowswith_missing is true disallowmissing! is called on the merged DataFrame which may convert missing to non-missing where possible or error if conversion is not possible.\n\nExample\n\njulia> df_shapes = simulate_raster() |> x -> simulate_shapes(x);\n\njulia> df_layout = simulate_layout(df_shapes);\n\njulia> fname_shapes = write_shapes(df_shapes, path=\"simulated.shp\", overwrite=true);\n\njulia> fname_layout = write_layout(df_layout, path=\"simulated-layout.tsv\", overwrite=true);\n\njulia> df_shapes_reloaded = load_shapes_merge_layout(fname_shapes);\n\njulia> df_shapes_reloaded == df_shapes\ntrue\n\njulia> df_shapes_merged_layout = load_shapes_merge_layout(fname_shapes, fname_layout=fname_layout, id=\"id\", name=\"name\");\n\njulia> names(df_shapes_merged_layout) == sort(unique(vcat(names(df_shapes), names(df_layout))))\ntrue\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.simulate-Tuple{}","page":"Home","title":"CropPhenoFromDrones.simulate","text":"simulate(;\n    lon_ini::Float64 = 25.0,\n    lon_fin::Float64 = 30.0,\n    lon_step::Float64 = 1.0,\n    lat_ini::Float64 = 25.0,\n    lat_fin::Float64 = 30.0,\n    lat_step::Float64 = 1.0,\n    EPSG_code::Int64 = 32754,\n    n_time_points::Int64 = 1,\n    μ::Float64 = 0.0,\n    σ::Float64 = 1.0,\n    bands::Vector{String} = [\"red\", \"green\", \"blue\", \"nir\"],\n    save::Bool = true,\n    fname_prefix::String = \"simulated\",\n    overwrite::Bool = false,\n    seed::Int64 = 42,\n    verbose::Bool = false,\n)::Tuple{Dict{String,Raster},DataFrame,DataFrame,Union{Nothing,Vector{String}}}\n\nCreate synthetic multi-band raster data and associated vector outputs for a simple simulated field experiment.\n\nThis function:\n\nsimulates one Raster per requested band over a regular lon/lat grid (projected to the supplied EPSG),\nderives per-cell polygon geometries (shapefile-like) from the first simulated band,\nderives a simple layout table mapping plot ids to entry names and spatial centres,\noptionally writes GeoTIFFs (one per band), the shapefile (with auxiliary files), and a layout TSV to disk.\n\nArguments\n\nlonini, lonfin, lonstep, latini, latfin, latstep: grid extent and spacing (ranges use Julia start:step:stop semantics).\nEPSG_code: integer EPSG code used to set the raster CRS.\nntimepoints: number of temporal layers per band (see notes below).\nμ, σ: Normal(μ, σ) parameters used to generate pixel values.\nbands: names of bands to simulate; returned as keys in the channels Dict.\nsave: when true, write outputs to disk and return a Vector of written file paths; when false, return nothing for the filenames element.\nfname_prefix: prefix used for output filenames when saving.\noverwrite: allow overwriting existing files when saving.\nseed: integer seed passed to simulate_raster for reproducible random draws.\nverbose: when true, print progress messages and show progress meters.\n\nReturns\n\nA 4-tuple:\n\nDict{String,Raster} — mapping band name to the simulated Rasters.Raster object.\nDataFrame — per-cell polygon table (columns include id and geometry).\nDataFrame — layout table mapping id to name, row, column, block.\nUnion{Nothing,Vector{String}} — when save == true a Vector of file paths written; otherwise nothing.\n\nNotes\n\nShapes and layout are derived from the first band in bands. Ensure the first band is the one you want to use for geometry/layout derivation.\nWhen save == true the function writes one GeoTIFF per band, the shapefile components (.shp, .shx, .prj, .dbf) and a layout TSV file; the returned filename vector lists the produced files.\nThe seed value is passed unchanged to each call of simulate_raster. If you require different random draws across bands, vary seed per band before calling or change the call pattern.\nn_time_points > 1 triggers creation of a time axis in the simulated rasters; note that the simple helper currently attaches a single timestamp (see simulate_raster docs) so the effective number of distinct time slices may be limited.\nUse verbose=true to observe progress bars (requires ProgressMeter).\n\nExamples\n\njulia> channels, df_shapes, df_layout, fnames = simulate(overwrite=true);\n\njulia> first(channels)[end] isa Raster\ntrue\n\njulia> prod(size(first(channels)[end].data) .- 1) == nrow(df_shapes)\ntrue\n\njulia> nrow(df_shapes) == nrow(df_layout)\ntrue\n\njulia> length(fnames) == 9\ntrue\n\njulia> sum(.!isnothing.(match.(Regex(\".tiff\"), fnames))) == 4\ntrue\n\njulia> sum(.!isnothing.(match.(Regex(\".shp|.shx|.prj|.dbf\"), fnames))) == 4\ntrue\n\njulia> sum(.!isnothing.(match.(Regex(\"-layout.tsv\"), fnames))) == 1\ntrue\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.simulate_layout-Tuple{DataFrames.DataFrame}","page":"Home","title":"CropPhenoFromDrones.simulate_layout","text":"simulate_layout(df_shapes::DataFrame; max_replications::Int64 = 2)::DataFrame\n\nGenerate a simple plot/layout table from a spatial DataFrame that contains an id field and an geometry field (ArchGDAL geometries). The function computes a representative centre (X, Y) for each geometry and assembles a layout DataFrame with one row per input feature.\n\nArguments\n\ndf_shapes::DataFrame: A spatial DataFrame containing at least the columns id and geometry (ArchGDAL polygon-like geometries). The implementation expects polygon-like geometries where points at indices 0 and 2 represent opposite corners (used to compute the centre).\nmax_replications::Int64=2 (optional): Maximum number of replications per experimental unit. This value is used to compute the number of unique \"entry\" names; when max_replications > 1 the function will create fewer unique entry labels and assign them across the rows (names are sampled for assignment).\n\nReturns\n\nDataFrame: A tabular layout with columns id (copied from the input DataFrame), name (an assigned entry label, e.g. \"entry1\", \"entry2\", ...), row (X coordinate of the computed feature centre), column (Y coordinate of the computed feature centre), and block (a block identifier, currently the fixed string \"block_1\").\n\nErrors\n\nThrows an ErrorException if either the id or geometry columns are missing from the input DataFrame.\nThrows an ErrorException if max_replications < 1.\n\nNotes\n\nThe centre is computed as the midpoint between the geometry points at indices 0 and 2, so the geometry must be structured such that those indices correspond to opposite corners of the shape (typical for rectangular/axis-aligned polygons).\nThe name column is generated by sampling labels of the form \"entry_i\". To obtain deterministic names set the random seed prior to calling or post-process the name column.\nThe block column is currently a single constant value (\"block_1\"); extend as needed for multi-block experiments.\n\nExamples\n\njulia> raster = simulate_raster();\n\njulia> df_shapes = simulate_shapes(raster);\n\njulia> df_layout = simulate_layout(df_shapes, max_replications=3);\n\njulia> nrow(df_shapes) == nrow(df_layout)\ntrue\n\njulia> maximum(values(countmap(df_layout.name)))\n3\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.simulate_raster-Tuple{}","page":"Home","title":"CropPhenoFromDrones.simulate_raster","text":"simulate_raster(;\n    lon_ini::Float64=25.0,\n    lon_fin::Float64=30.0,\n    lon_step::Float64=1.0,\n    lat_ini::Float64=25.0,\n    lat_fin::Float64=30.0,\n    lat_step::Float64=1.0,\n    EPSG_code::Int64=32754, # used in the Fan's 2023 GeoRaster data\n    n_time_points::Int64=1,\n    μ::Float64=0.0,\n    σ::Float64=0.0,\n    seed::Int64=42,\n)::Raster\n\nCreate a synthetic Raster with spatial (longitude, latitude) dimensions and an optional time dimension. Raster cell values are sampled from a Normal distribution N(μ, σ) and the returned raster is assigned the coordinate reference system given by EPSG(EPSG_code) (see spatial reference list).\n\nArguments\n\nlon_ini::Float64=25.0: starting longitude (inclusive).\nlon_fin::Float64=30.0: ending longitude (inclusive).\nlon_step::Float64=1.0: spacing between longitude samples.\nlat_ini::Float64=25.0: starting latitude (inclusive).\nlat_fin::Float64=30.0: ending latitude (inclusive).\nlat_step::Float64=1.0: spacing between latitude samples.\nEPSG_code::Int64=32754: EPSG code used to set the raster CRS.\nn_time_points::Int64=1: number of time slices requested. If > 1 a time dimension is added; the implementation creates a time index containing Dates.now() (a single timestamp) for the time dimension.\nμ::Float64=0.0: mean of the Normal distribution used to generate raster values.\nσ::Float64=1.0: standard deviation of the Normal distribution.\nseed::Int64=42: integer seed used with Random.seed! to make sampling reproducible.\n\nReturns\n\nRaster: a Rasters.Raster object with dimensions corresponding to the specified longitude and latitude ranges. If n_time_points > 1, the raster has dimensions (lon, lat, time); otherwise it has (lon, lat).\n\nNotes\n\nLongitude and latitude coordinates are generated using Julia range notation lon_ini:lon_step:lon_fin and lat_ini:lat_step:lat_fin, so end values are included when they fall on the step sequence. Invalid steps (e.g., zero) or ranges that produce empty coordinate vectors will raise an error.\nValues are drawn with rand(N, ...) where N = Distributions.Normal(μ, σ). Set seed for deterministic output across calls.\nWhen n_time_points > 1 the function currently attaches a single timestamp (Dates.now()) as the time index; the length of the time dimension in the returned raster may therefore be 1 even if n_time_points is larger. (If you require multiple distinct time slices, generate or supply an explicit time index.)\n\nExamples\n\njulia> raster_1 = simulate_raster();\n\njulia> raster_2 = simulate_raster();\n\njulia> raster_3 = simulate_raster(lon_ini = 10.0);\n\njulia> abs(mean(raster_1.data)) < 0.5\ntrue\n\njulia> abs(1.00 - std(raster_1.data)) < 0.5\ntrue\n\njulia> raster_1 == raster_2\ntrue\n\njulia> raster_1 == raster_3\nfalse\n\njulia> prod(size(raster_1.data)) < prod(size(raster_3.data))\ntrue\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.simulate_shapes-Tuple{Rasters.Raster}","page":"Home","title":"CropPhenoFromDrones.simulate_shapes","text":"simulate_shapes(raster::Raster)::DataFrame\n\nConstruct polygon geometries for each cell of a raster and return a DataFrame mapping plot ids to polygon geometries.\n\nArguments\n\nraster::Raster: A Rasters.jl-compatible raster object with X and Y dimensions and an associated coordinate reference system (CRS); the function uses the raster grid coordinates to build polygons for each cell.\n\nReturns\n\nDataFrame: A DataFrame with columns sorted alphabetically. These columns are id (String) and geometry (ArchGDAL polygon objects) representing per-cell polygons; where the CRS of the raster is assigned to the returned DataFrame via GeoDataFrames.setcrs!.\n\nDetails\n\nEach polygon corresponds to a rectangular cell formed by consecutive X and Y grid coordinates derived from dims(raster, Rasters.X).val and dims(raster, Rasters.Y).val\nCell corners are ordered to close the polygon\nIDs are generated as \"plot1\", \"plot2\", ...\nArchGDAL.createpolygon is used to construct geometries\n\nNotes\n\nThe function assumes a regular raster grid, presence of ArchGDAL and GeoDataFrames functionality, and that the geometry column is named geometry (Shapefile exports impose a 10-character column name limit).\n\nExamples\n\njulia> raster = simulate_raster();\n\njulia> df_shapes = simulate_shapes(raster);\n\njulia> prod(size(raster.data) .- 1) == nrow(df_shapes)\ntrue\n\njulia> names(df_shapes) == sort([\"id\", \"geometry\"])\ntrue\n\njulia> typeof(df_shapes.id) == Vector{String}\ntrue\n\njulia> typeof(df_shapes.geometry) == Vector{ArchGDAL.IGeometry{ArchGDAL.wkbPolygon}}\ntrue\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.write_layout-Tuple{DataFrames.DataFrame}","page":"Home","title":"CropPhenoFromDrones.write_layout","text":"write_layout(df_layout::DataFrame; path::String, overwrite::Bool=false)::String\n\nWrite a layout DataFrame to a tab-separated values (TSV) file and return the written filepath.\n\nArguments\n\ndf_layout::DataFrame contains the layout rows and columns to serialize; path::String is the desired output filepath; overwrite::Bool indicates whether to replace an existing file.\n\nReturns\n\nThe full path of the written TSV file as a String.\n\nNotes\n\nThe function uses output_fname to resolve the filename and CSV.write with a tab delimiter to write the table, so ensure column types are serializable by CSV.\n\nExample\n\njulia> df_layout = simulate_raster() |> x -> simulate_shapes(x) |> x -> simulate_layout(x);\n\njulia> write_layout(df_layout, path=\"simulated-layout.tsv\", overwrite=true) |> basename\n\"simulated-layout.tsv\"\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.write_raster-Tuple{Rasters.Raster}","page":"Home","title":"CropPhenoFromDrones.write_raster","text":"write_raster(raster::Raster; path::String, overwrite::Bool=false)::String\n\nWrite a Raster to disk as a GeoTIFF and return the written filepath.\n\nArguments\n\nraster::Raster is the raster to write; path::String is the desired output filepath; overwrite::Bool indicates whether to replace an existing file.\n\nReturns\n\nThe full path of the written GeoTIFF as a String.\n\nNotes\n\nThe function resolves the final filename using output_fname and writes the data with Rasters.write, so ensure the provided raster is compatible with Rasters.jl and that necessary file drivers are available.\n\nExample\n\njulia> raster = simulate_raster();\n\njulia> write_raster(raster, path=\"simulated.tiff\", overwrite=true) |> basename\n\"simulated.tiff\"\n\n\n\n\n\n","category":"method"},{"location":"#CropPhenoFromDrones.write_shapes-Tuple{DataFrames.DataFrame}","page":"Home","title":"CropPhenoFromDrones.write_shapes","text":"write_shapes(df_shapes::DataFrame; path::String, overwrite::Bool=false)::String\n\nWrite a DataFrame of geometries and attributes to a shapefile and return the written filepath.\n\nArguments\n\ndf_shapes::DataFrame is the GeoDataFrame-like table of shapes and attributes to write; path::String is the desired output filepath; overwrite::Bool indicates whether to replace an existing file.\n\nReturns\n\nThe full path of the written shapefile as a String.\n\nNotes\n\nThe function uses output_fname to compute the destination filename and GeoDataFrames.write to perform the write, so ensure the DataFrame conforms to the expected geometry column and CRS conventions.\n\nExample\n\njulia> df_shapes = simulate_raster() |> x -> simulate_shapes(x);\n\njulia> write_shapes(df_shapes, path=\"simulated.shp\", overwrite=true) |> basename\n\"simulated.shp\"\n\n\n\n\n\n","category":"method"}]
}
