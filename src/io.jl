function load_raster(path::String) # -> GeoRaster / GeoArray
    # TODO
end

function load_hyperspec(path::String) # -> HyperspectralCube (with wavelengths)
    # TODO
end

function load_pointcloud(path::String) # -> PointCloud (with XYZ, intensity, return info)
    # TODO
end
